<Page x:Class="TeachAssistApp.Views.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="900"
      Loaded="Page_Loaded">

    <Grid Background="{DynamicResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="32,28,32,16">
            <TextBlock Text="My Grades"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Pierre Elliott Trudeau High School"
                       FontSize="14"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Stats Row -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="32,0,32,20" VerticalAlignment="Bottom">
            <ui:CardControl Margin="0,0,12,0">
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="Average" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        <TextBlock Text="{Binding AverageMark, StringFormat={}{0:F1}%}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:SymbolIcon Symbol="ChartMultiple24" Margin="0,0,16,0"/>
            </ui:CardControl>

            <ui:CardControl Margin="0,0,12,0">
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="GPA" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        <TextBlock Text="{Binding Gpa}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:SymbolIcon Symbol="People24" Margin="0,0,16,0"/>
            </ui:CardControl>

            <ui:CardControl>
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="Courses" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        <TextBlock Text="{Binding CourseCount}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:SymbolIcon Symbol="Book24" Margin="0,0,16,0"/>
            </ui:CardControl>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ui:Button Content="Refresh" Command="{Binding LoadCoursesCommand}" Appearance="Primary" Icon="{ui:SymbolIcon ArrowSync24}" Margin="0,0,8,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Course List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="32,0,32,32">
            <ItemsControl x:Name="CourseItemsControl" ItemsSource="{Binding Courses}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:CardAction Margin="0,0,0,8" MinHeight="72" Padding="4"
                                        Command="{Binding DataContext.SelectCourseCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                            <Grid Margin="8,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding Code}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="16,0,0,0"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Grid.Column="2"
                                           Text="{Binding DisplayMark}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="{Binding GradeColor, Converter={StaticResource StringToBrushConverter}}"
                                           VerticalAlignment="Center"
                                           Margin="16,0,0,0"/>

                                <TextBlock Grid.Column="3"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="16,0,0,0">
                                    <Run Text="P"/><Run Text="{Binding Block}"/>
                                </TextBlock>

                                <TextBlock Grid.Column="4"
                                           Text="{Binding Room}"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="16,0,0,0"/>
                            </Grid>
                        </ui:CardAction>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="2"
              Background="#AA000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="48" Height="48" Margin="0,0,0,16"/>
                <TextBlock Text="Loading grades..."
                           Foreground="White"
                           FontSize="16"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Empty State -->
        <Grid Grid.Row="1"
              Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:SymbolIcon Symbol="BookQuestionMark24" FontSize="48"
                                   Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="No Courses Found" FontSize="20" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Your courses will appear here once marks are published" FontSize="14"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <ui:Button Content="Refresh" Command="{Binding LoadCoursesCommand}" Appearance="Primary" Icon="{ui:SymbolIcon ArrowSync24}"/>
            </StackPanel>
        </Grid>

        <!-- Error Message -->
        <ui:InfoBar Grid.Row="1"
                    Title="Error"
                    Message="{Binding ErrorMessage}"
                    Severity="Error"
                    IsOpen="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
                    Margin="32,60,32,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    MaxWidth="500"/>
    </Grid>
</Page>
